/**
 * 将下面路由组件部分复制到 /src/router/index.ts 中
 */
const routes: Array<RouteRecordRaw> = [

    {
        path: '/${menuUrl}',
        component: () => import('@/views/${moduleName}/${lowercaseName}/${className}.vue'),
        name: '${className}Management',
        meta: {
            title: '${comments}'
        }
    },

]